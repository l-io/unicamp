#include<stdio.h>

int main()
{
	int A, B, C, D, E, F ,X, N, M;
	printf("Digite a data desejada e os tres numeros separados por espacos:");
	scanf("%4d/%4d/%4", &A, &B, &C);
	printf("Os numeros sao: %d/%d/%d ", A, B, C);
	return 0;
}
